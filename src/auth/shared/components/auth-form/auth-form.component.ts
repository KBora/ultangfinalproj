import { Component } from '@angular/core';

@Component({
  selector: 'auth-form',
  styleUrls: ['auth-form.component.scss'],
  template: `
    <div>
     Authentication Form
    </div>
  `
})
export class AuthFormComponent {
  constructor() {}
}
